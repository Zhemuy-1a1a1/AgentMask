# Agent 文件精简报告（2026-03-22）

## 最长5个文件分析

| 文件 | 行数 | 主要问题 |
|---|---|---|
| 销售台账Agent.md | 889 | 示例过多（子文件夹结构当内容展示）、README式引导段落过长 |
| 对账Agent.md | 690 | 多角色示例堆砌、表格和流程描述重复 |
| 读书陪跑Agent.md | 605 | 章节层级过深、分支场景过多 |
| 周报生成Agent.md | 509 | 输出示例冗长、背景描述与指令重复 |
| 知识库问答Agent.md | 503 | 术语解释过长、参数说明过于细致 |

---

## 具体精简建议

### 销售台账Agent.md（889行 → 目标500行，压缩44%）

**可删减：**
1. 子文件夹结构展示（`├── README.md` 那段）：约80行，直接删或压缩成一句话
2. "交付内容"章节下的完整目录树：约60行，保留路径名即可
3. 重复的价值强调段落（"不是普通记表工具"说了3遍）：合并为1段，约40行
4. 多个子agent的完整说明（如"客户管理Agent"、"订单管理Agent"独立段落）：每个约50行，只保留摘要
5. 早期版本说明/changelog：约50行，移到最后或删

**精简后结构：**
- 开场：核心功能一句话 + 适用场景（5行）
- 适用人群（表格，10行）
- 交付内容目录（保留路径，20行）
- Agent主体指令（保留核心逻辑，删除子模块展开，300行）
- 使用示例（保留2个核心场景，100行）

---

### 对账Agent.md（690行 → 目标430行，压缩38%）

**可删减：**
1. 多角色对账流程（老板娘/会计/老板 分别写了完整流程）：只保留通用流程，删除分角色展开，约120行
2. 表格示例过于详细（每种表格都给了完整行列数据）：压缩成"表结构说明"，约60行
3. 异常处理的长篇描述：删除边界情况，仅保留3个最高频异常，约80行
4. 术语解释（每出现一个新词就有一段解释）：只保留核心术语表，压缩约50行

---

### 读书陪跑Agent.md（605行 → 目标380行，压缩37%）

**可删减：**
1. 四阶段详细展开（每阶段都写了"为什么重要"）：只保留执行步骤，删除"阶段意义"，约100行
2. 读后感模板过多（给出了5种不同风格的读后感模板）：保留2种，删除3种，约80行
3. 分支场景过多（考研人/职场人/家长等分别展开）：合并为"不同人群调整方法"一段，约60行
4. 附录FAQ（15个Q&A）：保留5个最高频问题，其余删，约50行

---

### 周报生成Agent.md（509行 → 目标330行，压缩35%）

**可删减：**
1. 周报模板示例过于详细（给出了4种不同格式的完整示例）：保留2种，删除2种，约80行
2. 背景描述（"为什么要写周报"说了两遍）：删除重复，约30行
3. 补充说明章节（大量"注意事项"分散在各节）：合并为一个"注意事项"节，约40行
4. 输出字段逐个解释（每个字段都有2-3行解释）：改为字段表，压缩约40行

---

### 知识库问答Agent.md（503行 → 目标330行，压缩34%）

**可删减：**
1. 技术实现细节描述（embedding/检索原理展开）：只保留使用说明，删实现原理，约80行
2. 参数说明过于详细（每个参数都有3-4行说明）：改为简短参数表，约60行
3. 多种检索策略展开（语义/关键词/混合/重排序）：只保留最终推荐的混合策略，删策略对比，约60行
4. FAQ（12个Q&A）：保留5个核心问题，其余删，约40行

---

## 统一头部模板（已完成）

已为12个核心Agent文件添加YAML front matter：
```yaml
---
name: agent-name
description: Agent 功能简述
use_case: 使用场景
target_user: 目标用户
version: "1.0.0"
updated: "2026-03-22"
tokens_estimate: "Xk-Yk"
---
```

## 下一步

精简前建议先用`examples/sample-inputs.md`验证Agent输出质量，精简后保持输出格式不变。
